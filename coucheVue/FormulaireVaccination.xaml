<Window x:Class="RefugeAnimaux.coucheVue.FormulaireVaccination"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vaccinations" Height="620" Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Info animal (read-only) -->
        <GroupBox Grid.Row="0" Header="Animal" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Nom :" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtNom" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" FontWeight="Bold"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Type :" VerticalAlignment="Center" Margin="0,5,0,0"/>
                <TextBlock x:Name="txtType" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="0,5,0,0"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Identifiant :" VerticalAlignment="Center" Margin="0,5,0,0"/>
                <TextBlock x:Name="txtIdentifiant" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center" Margin="0,5,0,0"/>
            </Grid>
        </GroupBox>

        <!-- Vaccinations existantes -->
        <GroupBox Grid.Row="1" Header="Vaccinations enregistrees" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <DataGrid x:Name="dgVaccinations"
                          Grid.Row="0"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionChanged="DgVaccinations_SelectionChanged"
                          MinHeight="150"
                          Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Vaccin" Binding="{Binding Vaccin}" Width="*"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=dd/MM/yyyy}" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>
                <Button x:Name="btnModifier" Grid.Row="1" Content="Modifier" Click="BtnModifier_Click"
                        Padding="10,5" Margin="5,5,5,5" HorizontalAlignment="Right" IsEnabled="False"/>
            </Grid>
        </GroupBox>

        <!-- Ajouter / Modifier une vaccination -->
        <GroupBox x:Name="grpFormulaire" Grid.Row="2" Header="Ajouter une vaccination" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Vaccin :" VerticalAlignment="Center"/>
                <ComboBox x:Name="cmbVaccin" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Date :" VerticalAlignment="Center" Margin="0,5,0,0"/>
                <DatePicker x:Name="dpDateVaccin" Grid.Row="1" Grid.Column="1" Margin="0,5,0,0"/>
                <StackPanel Grid.Row="1" Grid.Column="2" Orientation="Horizontal" Margin="10,5,0,0">
                    <Button x:Name="btnAjouter" Content="Ajouter"
                            Click="BtnAjouter_Click" Padding="15,5"/>
                    <Button x:Name="btnAnnulerModif" Content="Annuler" Click="BtnAnnulerModif_Click"
                            Padding="10,5" Margin="5,0,0,0" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Bouton fermer -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Fermer" Click="BtnFermer_Click" Padding="15,5"/>
        </StackPanel>
    </Grid>
</Window>
